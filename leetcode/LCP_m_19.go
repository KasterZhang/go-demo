package leetcode

//TODO
func minimumOperations(leaves string) int {
	result, start, end := 0, 0, len(leaves)-1
	cly, clr, cry, crr := 0, 0, 0, 0
	for start != end && start+1 != end {
		if
		start++
		end--
	}
	return result
}
